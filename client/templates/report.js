Session.set("showLog", true);

/*
Template.dev.events({
	"click .hide-dev": function(e) {

		if (Session.get("showLog")) {
			Template.instance().$('.data-log').css('bottom','-290px');

			Session.set("showLog", false);

		} else {
			Template.instance().$('.data-log').css('bottom', '0px');

			Session.set("showLog", true);
		}


	}
});


Template.dev.helpers({
	"toggleLog": function() {
		return Session.get("showLog") ? "Hide Developer Log" : "Show Developer Log";
	},

	"datapoints": function() {
		return query;
	}

});
*/